<br>
<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
  <%= (link_to_if (current_user.admin? && !current_user?(user)), 
  "| delete", user, method: :delete, data: {confirm: "are you sure?"}) %>
</li>
<div class="clear"></div>