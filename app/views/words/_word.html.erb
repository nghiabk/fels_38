<tr class="word_row">
  <td><%= word.content %></td>    
  <td><%= word.meaning %></td>
  <td>
    <%= link_to "Edit", edit_word_path(word) %> / 
    <%= link_to "Delete", word, method: :delete, data: {confirm: "You sure?"} %>
  </td>
</tr>